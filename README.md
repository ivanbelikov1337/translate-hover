# AutoTranslate Hover 🌐

Просте браузерне розширення для автоматичного перекладу тексту при наведенні курсора.

## ✨ Можливості

- 🖱️ **Переклад при наведенні** - наведіть курсор на будь-яке слово для миттєвого перекладу
- 🌍 **Підтримка багатьох мов** - українська, англійська, німецька, французька та інші
- ⚡ **Швидко та легко** - переклад з'являється за 0.5 секунди
- 🎨 **Сучасний дизайн** - красивий tooltip з градієнтом
- ⚙️ **Налаштування** - вибір мови перекладу та можливість вимкнення

## 🚀 Встановлення

### Для розробки

1. Клонуйте репозиторій:
```bash
git clone <repo-url>
cd hover-translater
```

2. Встановіть залежності:
```bash
npm install
```

3. Зберіть проект:
```bash
npm run build
```

4. Завантажте розширення в Chrome:
   - Відкрийте `chrome://extensions/`
   - Увімкніть "Режим розробника"
   - Натисніть "Завантажити розпаковане"
   - Виберіть папку `dist`

## 🛠️ Розробка

```bash
# Запуск dev-сервера для popup
npm run dev

# Збірка для продакшену
npm run build

# Перевірка коду
npm run lint
```

## 📁 Структура проекту

```
hover-translater/
├── public/
│   ├── manifest.json      # Маніфест розширення
│   ├── content.js         # Content script (переклад на сторінках)
│   ├── content.css        # Стилі tooltip
│   ├── background.js      # Service worker
│   └── icons/             # Іконки розширення
├── src/
│   ├── App.tsx            # Popup компонент
│   ├── App.css            # Стилі popup
│   └── main.tsx           # Entry point
└── ...
```

## 🔧 Як це працює

1. **Content Script** (`content.js`) - впроваджується на кожну веб-сторінку
2. При наведенні курсора на слово, скрипт:
   - Визначає слово під курсором
   - Відправляє запит до API перекладу (MyMemory)
   - Показує tooltip з перекладом
3. **Popup** (`App.tsx`) - дозволяє налаштувати розширення
4. **Background** (`background.js`) - керує налаштуваннями

## 🌐 API перекладу

Використовується безкоштовний API [MyMemory](https://mymemory.translated.net/):
- Ліміт: 5000 символів/день для анонімних користувачів
- Підтримка автовизначення мови

## 📝 Ліцензія

MIT License
